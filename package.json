{
  "name": "kamilogorek",
  "version": "1.0.0",
  "description": "Hello there!",
  "main": "deploy.js",
  "scripts": {
    "deploy": "bundle exec jekyll build && node deploy.js"
  },
  "devDependencies": {
    "gh-pages": "^0.12.0"
  }
}
