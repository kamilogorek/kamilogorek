{
  "name": "kamilogorek",
  "version": "1.0.0",
  "description": "Hello there!",
  "main": "deploy.js",
  "scripts": {
    "deploy": "rm -rf _site && bundle exec jekyll build && cp -r ../nutrition-database/dist _site/nutrition-database && node deploy.js"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}